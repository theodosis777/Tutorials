const countingChars=(str)=>{
    let charsList={}
    let maxcount=0;
    let maxchar='';

    for (i=0;i<str.length;i++){
        if (typeof(charsList[str[i]])!=undefined){

        }else{
            charsList[str[i]])=1;
        }
    }


}